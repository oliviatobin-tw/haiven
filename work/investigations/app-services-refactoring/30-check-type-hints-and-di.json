{"desc": "Verify type hints and constructor dependency injection compliance", "context": "Check all 18 service files for: (1) missing type hints on function parameters and return values; (2) classes that do not accept dependencies via constructors (i.e. create their own dependencies internally). Note any use of module-level globals or singletons that bypass DI.", "acceptance_criteria": ["All files checked for type hint coverage", "All classes checked for constructor DI", "Missing type hints listed by file:line", "DI violations listed", "Findings ready for report"]}
