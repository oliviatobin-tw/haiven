{"desc": "Split chats.py into focused module files", "context": "app/llms/chats.py (498 lines) contains 6 distinct classes: HaivenBaseChat, StreamingChat, JSONChat, ServerChatSessionMemory, ChatOptions, ChatManager (evidence: size-01). Split into: base_chat.py (HaivenBaseChat + ChatOptions), streaming_chat.py (StreamingChat), json_chat.py (JSONChat), session_memory.py (ServerChatSessionMemory), chat_manager.py (ChatManager). Keep chats.py as a re-export shim (from .base_chat import ..., etc.) to avoid breaking existing imports. Verify all referencing files still work.", "acceptance_criteria": ["chats.py is under 50 lines (re-exports only)", "Each new file contains exactly its designated class(es)", "All existing imports from llms.chats continue to work", "cd app && poetry run pytest -x --ignore=tests/test_server.py passes"]}
