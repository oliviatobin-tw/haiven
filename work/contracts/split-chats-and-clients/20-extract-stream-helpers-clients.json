{"desc": "Extract helpers from ChatClient.stream() in clients.py", "context": "app/llms/clients.py: stream() method (lines 112-179) is 68 lines handling streaming, citation extraction, usage data, mock switching, error handling, and response normalization (evidence: size-02). Extract into private helpers: _extract_citations(chunk) -> optional citations, _extract_usage(chunk) -> optional usage data, _emit_content(chunk) -> content string. The stream() method should orchestrate these helpers and remain under 30 lines.", "acceptance_criteria": ["stream() method is under 30 lines", "At least 2 private helper methods extracted (_extract_citations or similar)", "cd app && poetry run pytest -x --ignore=tests/test_server.py passes", "No behavior change \u2014 all streaming tests pass"]}
