{"desc": "Extract inline prompt construction from api_basics.py iterate() and api_multi_step.py", "context": "app/api/api_basics.py iterate() (lines 446-468) and app/api/api_multi_step.py kick_off_explore() (lines 101-138) and generate_follow_up() (lines 62-67) build multi-paragraph LLM prompts inline using f-strings with user-supplied input (evidence: soc-02, soc-03, sec-01, sec-02, sec-03). Extract each prompt construction to a dedicated builder function (e.g., _build_iterate_prompt(userinput, scenarios) in api_basics.py; _build_explore_prompt(...) in api_multi_step.py). This isolates the injection surface and reduces handler size. Add a # SECURITY: user input interpolated here comment at each interpolation point as a minimum visible marker.", "acceptance_criteria": ["iterate() handler body is under 30 lines", "kick_off_explore() handler body is under 30 lines", "Each prompt builder is a standalone function that can be unit-tested", "# SECURITY comment present at each user-input interpolation point", "cd app && poetry run pytest tests/test_api.py -x passes"]}
