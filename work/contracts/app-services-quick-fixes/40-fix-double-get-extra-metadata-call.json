{"desc": "Fix double get_extra_metadata() call in DocumentsUtils", "context": "app/embeddings/documents.py lines 121-122: get_search_result_item() calls get_extra_metadata(document_metadata) twice \u2014 once in the if-condition and once in the f-string (smell-04). Assign the result to a local variable first: extra = DocumentsUtils.get_extra_metadata(document_metadata), then use extra in both places.", "acceptance_criteria": ["get_extra_metadata() is called exactly once in get_search_result_item()", "Result is stored in a local variable and reused", "cd app && poetry run pytest -x --ignore=tests/test_server.py passes"]}
